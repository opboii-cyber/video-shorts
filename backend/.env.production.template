# ═══════════════════════════════════════════════════════════════
# Video Shorts SaaS Backend — Production Variables
# ═══════════════════════════════════════════════════════════════
# Instructions: Add these to your Render, Railway, or VPS environment

# ── Next.js Frontend URL (for CORS and Polar Webhooks) ──────
FRONTEND_URL=https://yourdomain.com

# ── AI API Keys ──────────────────────────────────────────────
OPENAI_API_KEY=sk-...
ANTHROPIC_API_KEY=sk-ant-...

# ── Polar Production Payments ──────────────────────────────────
# Use your Live Access Token
POLAR_ACCESS_TOKEN=polar_pat_...
POLAR_WEBHOOK_SECRET=whsec_...
# Your Live Product IDs
POLAR_STARTER_PRODUCT_ID=prod_...
POLAR_PRO_PRODUCT_ID=prod_...
POLAR_AGENCY_PRODUCT_ID=prod_...

# ── Production Database (Supabase / Neon / Render DB) ────────
# Note: Ensure the Postgres URL starts with `postgresql://` (not `postgres://` for SQLAlchemy)
DATABASE_URL=postgresql://user:password@host:5432/db_name

# ── Production Redis Queue (Upstash / Render Redis) ──────────
REDIS_URL=redis://default:password@host:6379

# ── Cloud Storage (AWS S3 / Cloudflare R2) ───────────────────
S3_BUCKET=your-bucket-name
S3_REGION=auto
# If using Cloudflare R2, uncomment and fill this:
# S3_ENDPOINT=https://<account-id>.r2.cloudflarestorage.com
AWS_ACCESS_KEY_ID=your_access_key
AWS_SECRET_ACCESS_KEY=your_secret_key
